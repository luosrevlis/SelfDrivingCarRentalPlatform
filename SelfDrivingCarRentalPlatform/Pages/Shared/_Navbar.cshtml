@using BusinessObjects.Enums;
@if (User.Identity != null && User.Identity.IsAuthenticated)
{
    @if (User.IsInRole(UserRole.Admin.ToString()))
    {
        <li class="nav-item">
            <a class="nav-link" asp-page="/Admin/Index">Admin Home</a>
        </li>
    }
    @if (User.IsInRole(UserRole.CarOwner.ToString()))
    {
        <li class="nav-item">
            <a class="nav-link" asp-page="/CarOwners/Cars/Index">Your Cars</a>
        </li>
        
        <li class="nav-item">
            <a class="nav-link" asp-page="CarOwners/Contracts/RentedList">Renting History</a>
        </li>
    }
    @if (User.IsInRole(UserRole.Customer.ToString()))
    {
        <li class="nav-item">
            <a class="nav-link" asp-page="/Customer/Index">Customer Home</a>
        </li>

        <li class="nav-item">
            <a class="nav-link" asp-page="CarOwners/Contracts/RentedList">Rented History</a>
        </li>
    }
    @if (User.IsInRole(UserRole.Customer.ToString()) || User.IsInRole(UserRole.CarOwner.ToString()))
    {
        <li class="nav-item">
            <a class="nav-link" asp-page="/Cars/Index">Search Cars</a>
        </li>
        
        <li class="nav-item">
            <a class="nav-link" asp-page="/Contracts/Index">Renting History</a>
        </li>
        
        <li class="nav-item">
            <a class="nav-link" asp-page="/DrivingLicenses/Details">Driving License</a>
        </li>
    }
}
